// Q no. 16 - Write a program that prints the multiplication table of a given number up tp 10.

// Answer

let number = 5;
for (let i = 1; i <= 10; i++) {
  console.log(number + "x" + i + "=" + number * i);
}
